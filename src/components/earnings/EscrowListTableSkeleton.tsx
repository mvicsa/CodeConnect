import React from 'react';
import { TableSkeleton } from '@/components/ui/table-skeleton';

const EscrowListTableSkeleton = () => {
  return <TableSkeleton columns={4} rows={5} />;
};

export default EscrowListTableSkeleton;
