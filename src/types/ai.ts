export interface AISuggestion {
    suggestions: string
    postId: string
    _id: string
    createdAt: string
    updatedAt: string
}
  
export interface AIEvaluation {
    evaluation: string
    postId: string
    _id: string
    createdAt: string
    updatedAt: string
}